/** Límites de apuesta — fuente única para cliente y servidor. */
export const BET_AMOUNT_MIN = 1;
export const BET_AMOUNT_MAX = 5000;
export const MAX_CART_LINES = 40;
export const MAX_LOTTERIES_PER_LINE = 32;
/** Tope de seguridad por ticket (además del saldo del usuario). */
export const MAX_CART_TOTAL = 100_000;
